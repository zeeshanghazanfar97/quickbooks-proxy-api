# QuickBooks OAuth2 Credentials
# Get these from https://developer.intuit.com/app/developer/dashboard
QB_CLIENT_ID=your_client_id_here
QB_CLIENT_SECRET=your_client_secret_here

# QuickBooks Account Credentials (hardcoded)
# These are obtained from a one-time OAuth flow
# To get these tokens:
# 1. Visit https://developer.intuit.com/app/developer/playground
# 2. Complete OAuth flow to get access_token and refresh_token
# 3. Copy the realmId (Company ID) from the response
# 4. Add all three values below
QB_ACCESS_TOKEN=your_access_token_here
QB_REFRESH_TOKEN=your_refresh_token_here
QB_REALM_ID=your_realm_id_here

# QuickBooks Environment
# Options: sandbox or production
QB_ENVIRONMENT=sandbox

# OAuth2 Token URL (for token refresh)
QB_TOKEN_URL=https://oauth.platform.intuit.com/oauth2/v1/tokens/bearer

# Proxy Configuration
# Port for the proxy API server
PROXY_PORT=8000

# Proxy API Authentication
# Bearer token for authenticating requests to the proxy API
# If not set, the proxy will allow all requests (not recommended for production)
# Generate a secure token: python -c "import secrets; print(secrets.token_urlsafe(32))"
PROXY_BEARER_TOKEN=your_proxy_bearer_token_here
